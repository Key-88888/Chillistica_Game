# youtube-https profile

Status: placeholder only.

This profile must not be activated until:
- winws2 binary is added and verified;
- WinDivert handling is safe;
- START/STOP/KILL lifecycle is tested;
- rollback is implemented;
- arguments are reviewed against the exact bundled winws2 version.

First real target:
- YouTube HTTPS only;
- no UDP;
- no games;
- no global filter.

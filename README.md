# Chillistica_Game

Обход блокировок (DPI bypass) для YouTube, Discord, Roblox и Fortnite одной кнопкой на Windows 10/11 x64. Движок — winws ([zapret](https://github.com/bol-van/zapret)) + WinDivert.

Версия: **0.5.0**.

## Скачать

Последняя версия доступна на [странице релизов GitHub](https://github.com/Key-88888/Chillistica_Game/releases/latest).

Есть два варианта загрузки:

- `Chillistica_game-vX.Y.Z-win-x64.zip` — основной маленький архив, примерно 5–10 МБ, как zapret. Требует .NET 8 Desktop Runtime; если его нет, `run-first.cmd` установит среду автоматически.
- `Chillistica_game-vX.Y.Z-win-x64-standalone.zip` — архив крупнее, зато без внешних зависимостей и гарантированно запускается без установленного .NET.

### Если GitHub не открывается или архив не скачивается

Файлы релизов GitHub загружаются с `objects.githubusercontent.com`, который в России может резаться DPI. Можно использовать стороннее зеркало-прокси, добавив перед ссылкой на архив один из адресов:

- `https://ghproxy.net/` + ссылка на архив;
- `https://gh-proxy.com/` + ссылка на архив.

Например:

`https://ghproxy.net/https://github.com/Key-88888/Chillistica_Game/releases/download/v0.5.0/Chillistica_game-v0.5.0-win-x64.zip`

Зеркала — сторонние сервисы и не находятся под нашим контролем. Другие варианты: Cloudflare WARP, мобильный интернет или другая сеть, либо перенос архива с другого компьютера через USB.

## Установка и запуск

1. Скачайте подходящий ZIP-архив.
2. Распакуйте его в отдельную папку.
3. Запустите `run-first.cmd` двойным щелчком (или напрямую `Chillistica_game.exe`).
4. Подтвердите запрос контроля учётных записей (UAC).
5. Нажмите «Включить защиту».

**НЕТ службы, НЕТ установки — просто распаковали и запустили.** Приложение само запускает `winws.exe` с необходимыми правами.

## Антивирус

Антивирусы часто помечают `winws.exe` и WinDivert как PUA. Это нормально для инструментов такого класса: они перехватывают и изменяют сетевой трафик, чтобы обходить DPI.

Если антивирус удалил или заблокировал файлы, восстановите их из карантина и добавьте распакованную папку Chillistica_game в исключения.

## Обновления

Приложение проверяет GitHub и показывает баннер при выходе новой версии. Кнопка «Обновить» открывает страницу релизов; архив новой версии нужно скачать и распаковать вручную.

## Лицензии

Движок основан на zapret (winws) и WinDivert. Тексты лицензий находятся в `Engine/winws2/licenses/`.
